<view class="page form-page">
  <input class="input" placeholder="纪念日名称" value="{{title}}" bindinput="onTitleInput" />
  <picker mode="date" value="{{date}}" bindchange="onDateChange">
    <view class="date-field">
      <view class="date-label">纪念日期</view>
      <view class="date-value">{{date}}</view>
    </view>
  </picker>
  <textarea class="textarea" placeholder="备注" value="{{note}}" bindinput="onNoteInput" />
  <button class="primary-button save-button" bindtap="save">保存</button>
</view>
